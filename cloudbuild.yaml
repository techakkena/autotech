options:
  defaultLogsBucketBehavior: REGIONAL_USER_OWNED_BUCKET

steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/autotech-api:$COMMIT_SHA'
      - '.'

images:
  - 'gcr.io/$PROJECT_ID/autotech-api:$COMMIT_SHA'